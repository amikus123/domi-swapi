'use strict';

/**
 * dish-time-category service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::dish-time-category.dish-time-category');
