'use strict';

/**
 * ingredient-preference service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::ingredient-preference.ingredient-preference');
