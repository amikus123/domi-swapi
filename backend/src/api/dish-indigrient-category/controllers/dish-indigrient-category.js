'use strict';

/**
 *  dish-indigrient-category controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::dish-indigrient-category.dish-indigrient-category');
