'use strict';

/**
 * dish-indigrient-category router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::dish-indigrient-category.dish-indigrient-category');
