'use strict';

/**
 * dish-indigrient-category service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::dish-indigrient-category.dish-indigrient-category');
